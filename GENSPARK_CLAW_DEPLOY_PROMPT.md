# Genspark Claw デプロイ指示プロンプト

アップロードしたZIPファイルは、分譲マンション管理組合向けの「議案書自動作成・整合性チェック」React / TypeScript / Express アプリです。

以下の仕様を満たすように、ZIPを展開して内容確認、ビルド、デプロイ、動作検証を実施してください。

## 1. アプリ概要

- アプリ名：分譲マンション管理組合 議案書自動作成・整合性チェック
- フロントエンド：React / TypeScript / Vite
- バックエンド：Express / TypeScript
- AI連携：Gemini API
- 文書出力：docx パッケージによる Word 出力

主な機能：

- 過去総会議事録の登録・AI解析
- 今期総会議案書ドラフト作成
- 過去決議との整合性チェック
- 監査コメント付き Word 出力
- 担当者アカウント別ワークスペース保存
- 複数物件管理

## 2. 今回の重要改修仕様

過去総会議事録の追加・表示を「過去5回分」に限定しないように修正済みです。

必須要件：

1. 過去総会議事録DBに登録できる議事録件数を5回分に制限しないこと。
2. 登録済み議事録一覧は、対象マンションの議事録を全件表示すること。
3. 表示順は期の降順とすること。
4. 担当者アカウント別保存機能は維持すること。
5. 担当者Aと担当者Bの議事録DBが混在・上書きされないこと。
6. 1人の担当者が複数物件を管理でき、物件ごとに議事録DBを確認できること。
7. 保存後に再ログインしても、5件を超える議事録が保持・復元されること。
8. 整合性チェックでは、保存済みの過去議事録を対象にできること。

## 3. 既存の担当者別保存仕様

Expressサーバーに以下のAPIがあります。

- `POST /api/workspace/load`
- `POST /api/workspace/save`

保存データはサーバー側の `data/workspaces/` 配下にJSONとして保存されます。

ファイル名は、担当者アカウントID + 保護キーからSHA-256で生成されるため、担当者ごとに保存領域が分離されます。

## 4. デプロイ手順

1. ZIPを展開してください。
2. `package.json` を確認してください。
3. 必要に応じて `.env.example` を参照し、`GEMINI_API_KEY` を環境変数に設定してください。
4. 依存関係をインストールしてください。

```bash
npm install
```

5. 型チェックを実行してください。

```bash
npm run lint
```

6. ビルドを実行してください。

```bash
npm run build
```

7. 本番起動を確認してください。

```bash
NODE_ENV=production npm run start
```

8. Node / Expressサーバーが動作する構成として公開してください。

## 5. デプロイ後の動作確認項目

### 過去議事録の5件制限解除

1. 同一マンションに対して6件以上の過去総会議事録を登録する。
2. 登録済み一覧に6件以上が表示されることを確認する。
3. 期の降順で表示されることを確認する。
4. 保存後にログアウトし、同じ担当者ID + 同じ保護キーで再ログインする。
5. 6件以上の議事録が復元されることを確認する。

### 担当者アカウント分離

1. 担当者Aでログインする。
2. 複数の過去議事録を登録し、保存する。
3. 担当者Bでログインする。
4. 担当者Aの議事録DBが表示されないことを確認する。
5. 再度担当者Aでログインし、担当者Aの議事録DBが保持されていることを確認する。

### 複数物件管理

1. 同じ担当者アカウントで複数マンションを登録する。
2. 物件ごとに過去議事録DBを登録する。
3. 物件切替時に、対象物件の議事録だけが表示されることを確認する。


## H1表示名の修正

画面上のH1タグ表示は、ログイン前・ログイン後ともに以下へ統一済みです。

- 議案書自動作成・整合性チェック

ブラウザタイトルも同じ名称に変更済みです。

## 6. 注意事項

- `data/workspaces/` は本番環境で永続化される必要があります。
- デプロイ先が一時ファイルシステムの場合は、永続ディスクまたはDBへ移行してください。
- Gemini APIを使う機能には `GEMINI_API_KEY` が必要です。
- Viteのビルド時にチャンクサイズ警告が出る可能性がありますが、今回の機能修正とは別件です。

## 7. 報告してほしい内容

デプロイ後、以下を報告してください。

- 公開URL
- 実行したコマンド
- `npm install` / `npm run lint` / `npm run build` の結果
- 6件以上の議事録登録・保存・再読込テスト結果
- 担当者A・担当者Bの分離テスト結果
- `data/workspaces/` の永続化可否
- 発生した警告・エラー
- 追加修正した内容
